# ขั้นตอนการลบและสร้างตาราง budget_category_items ใหม่

## ⚠️ คำเตือน
ข้อมูลเดิมในตาราง `budget_category_items` จะหายหมด!

---

## ขั้นตอนที่ 1: ลบตารางเดิม

```bash
mysql -u root hr_budget -e "DROP TABLE IF EXISTS budget_category_items;"
```

**ผลลัพธ์ที่คาดหวัง**: ไม่มีข้อความ error

---

## ขั้นตอนที่ 2: สร้างตารางใหม่ (ตามโครงสร้างที่วางแผน)

```bash
mysql -u root hr_budget < database\migrations\022_add_hierarchy_to_category_items.sql
```

**ผลลัพธ์ที่คาดหวัง**: ไม่มีข้อความ error

---

## ขั้นตอนที่ 3: ตรวจสอบโครงสร้างตาราง

```bash
mysql -u root hr_budget -e "DESCRIBE budget_category_items;"
```

**ผลลัพธ์ที่ควรเห็น**:
```
+-----------+--------------+------+-----+---------+----------------+
| Field     | Type         | Null | Key | Default | Extra          |
+-----------+--------------+------+-----+---------+----------------+
| id        | int          | NO   | PRI | NULL    | auto_increment |
| name      | varchar(255) | NO   |     | NULL    |                |
| code      | varchar(100) | YES  |     | NULL    |                |
| parent_id | int          | YES  | MUL | NULL    |                |
| level     | tinyint      | NO   |     | 0       |                |
+-----------+--------------+------+-----+---------+----------------+
```

**ถ้าเห็นโครงสร้างตรงตามข้างบน** → ✅ สำเร็จ! ไปต่อขั้นตอนที่ 4

**ถ้าไม่ตรง** → ส่ง output ที่ได้ให้ฉันดู

---

## ขั้นตอนที่ 4: นำเข้าข้อมูลจาก CSV (Seeder)

```bash
php scripts\seed_budget_hierarchy.php
```

**ผลลัพธ์ที่คาดหวัง**:
```
Seeding completed.
```

**ถ้า error** → คัดลอก error message ส่งให้ฉัน

---

## ขั้นตอนที่ 5: ตรวจสอบข้อมูล

**นับจำนวนแถวทั้งหมด**:
```bash
mysql -u root hr_budget -e "SELECT COUNT(*) as total FROM budget_category_items;"
```
**คาดหวัง**: ประมาณ 80-90 แถว

**ดูข้อมูล 10 แถวแรก**:
```bash
mysql -u root hr_budget -e "SELECT id, name, parent_id, level FROM budget_category_items LIMIT 10;"
```

**ตรวจสอบ root items (parent_id = NULL)**:
```bash
mysql -u root hr_budget -e "SELECT id, name, level FROM budget_category_items WHERE parent_id IS NULL;"
```

---

## ✅ เมื่อทุกขั้นตอนสำเร็จ

แจ้งฉันว่า **"ทุกขั้นตอนสำเร็จ"** แล้วฉันจะ:
- อัปเดต documentation
- สรุปงานที่ทำเสร็จ
- อัปเดต PRP และ task.md

---

## 🔄 Rollback (ถ้าต้องการย้อนกลับ)

ถ้าต้องการคืนโครงสร้างเดิม (ก่อนที่จะรัน seeder):
```bash
mysql -u root hr_budget -e "DROP TABLE IF EXISTS budget_category_items;"
```
แล้วให้ฉันช่วยสร้างโครงสร้างเดิมกลับมา
