# 🔄 Handoff Document - Disbursement Form Implementation

**วันที่**: 2025-12-29 19:56  
**งานที่ต้องการ**: ฟอร์มบันทึกรายการเบิกจ่ายงบประมาณ  
**Status**: Budget Category Items Hierarchy Complete, Ready for Disbursement Forms

---

## ✅ งานพื้นฐานที่ทำเสร็จแล้ว

### 1. Budget Category Items (Hierarchy)
- ✅ ตาราง `budget_category_items` พร้อม (86 items, 6 levels)
- ✅ Model `BudgetCategoryItem` พร้อม (hierarchy + CRUD methods)
- ✅ 13 คอลัมน์ รวม admin columns (timestamps, soft delete, etc.)

---

## 📋 งานหลักที่ต้องทำ: **Disbursement Forms**

### เอกสารอ้างอิง (สำคัญ!)
| ไฟล์ | ประเภท | เนื้อหา |
|------|--------|---------|
| `research/disbursement_form_workflow.md` | Design Doc | Workflow, DB Schema, Routes ครบถ้วน |
| `research/wireframe_disbursement_form.html` | Wireframe | UI Design พร้อม Tab Navigation |

---

## 🎯 สิ่งที่ต้องสร้าง

### Phase 1: Database Tables (3 ตาราง)

#### 1. `expense_types` (ประเภทรายจ่าย)
```sql
CREATE TABLE expense_types (
    id INT AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(50) NOT NULL UNIQUE,
    name_th VARCHAR(255) NOT NULL,
    sort_order INT DEFAULT 0,
    is_active BOOLEAN DEFAULT TRUE
);

-- Seed data
INSERT INTO expense_types (code, name_th, sort_order) VALUES
('personnel', 'งบบุคลากร', 1),
('operation', 'งบดำเนินงาน', 2),
('investment', 'งบลงทุน', 3),
('subsidy', 'งบเงินอุดหนุน', 4),
('other', 'งบรายจ่ายอื่น', 5);
```

#### 2. `disbursement_headers` (Header)
```sql
CREATE TABLE disbursement_headers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    fiscal_year INT NOT NULL,
    month TINYINT NOT NULL,
    organization_id INT NOT NULL,
    record_date DATE NOT NULL,
    status ENUM('draft', 'submitted', 'approved') DEFAULT 'draft',
    created_by INT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (organization_id) REFERENCES organizations(id)
);
```

#### 3. `disbursement_details` (รายละเอียด)
```sql
CREATE TABLE disbursement_details (
    id INT AUTO_INCREMENT PRIMARY KEY,
    header_id INT NOT NULL,
    plan_id INT NULL,
    output_id INT NULL,
    activity_id INT NULL,
    expense_type_id INT NULL,
    item_0 DECIMAL(18,2) NULL,
    item_1 DECIMAL(18,2) NULL,
    item_2 DECIMAL(18,2) NULL,
    item_3 DECIMAL(18,2) NULL,
    item_4 DECIMAL(18,2) NULL,
    item_5 DECIMAL(18,2) NULL,
    notes TEXT NULL,
    FOREIGN KEY (header_id) REFERENCES disbursement_headers(id) ON DELETE CASCADE,
    FOREIGN KEY (plan_id) REFERENCES budget_plans(id),
    FOREIGN KEY (expense_type_id) REFERENCES expense_types(id)
);
```

---

### Phase 2: Models (3 Models)

- `src/Models/ExpenseType.php`
- `src/Models/DisbursementHeader.php`
- `src/Models/DisbursementDetail.php`

---

### Phase 3: Controller

**`src/Controllers/DisbursementController.php`**

Methods ที่ต้องมี:
```php
// Header CRUD
- index()          // แสดงตารางรายการทั้งหมด
- create()         // ฟอร์มสร้าง Header
- store()          // บันทึก Header
- show($id)        // ดูรายละเอียด + Details
- edit($id)        // แก้ไข Header
- update($id)      // อัปเดต Header
- destroy($id)     // ลบ Header

// Detail CRUD  
- createItem($id)  // ฟอร์มเพิ่ม Detail
- storeItem($id)   // บันทึก Detail
- editItem($id, $itemId)    // แก้ไข Detail
- updateItem($id, $itemId)  // อัปเดต Detail
- destroyItem($id, $itemId) // ลบ Detail

// API for Hierarchical Dropdown
- getOutputs()     // ดึง ผลผลิต/โครงการ ตาม plan_id
- getActivities()  // ดึง กิจกรรม ตาม output_id
```

---

### Phase 4: Views (6 Views)

```
resources/views/disbursements/
├── index.php           # ตารางรายการ
├── create.php          # ฟอร์มสร้าง Header
├── edit.php            # แก้ไข Header
├── show.php            # ดูรายละเอียด + ตาราง Details
└── items/
    ├── create.php      # ฟอร์มเพิ่ม Detail (Tab Navigation)
    └── edit.php        # แก้ไข Detail
```

**UI Design ใช้จาก**:
- `wireframe_disbursement_form.html` (Tab Navigation, Dark Theme)
- Dark theme: bg-slate-950, bg-slate-900
- Glassmorphism effect
- Tab colors: บุคลากร (blue), ดำเนินงาน (green), ลงทุน (purple), อุดหนุน (amber), อื่น (rose)

---

### Phase 5: Routes

```php
// Disbursement Routes
Router::get('/budgets/disbursements', [DisbursementController::class, 'index']);
Router::get('/budgets/disbursements/create', [DisbursementController::class, 'create']);
Router::post('/budgets/disbursements', [DisbursementController::class, 'store']);
Router::get('/budgets/disbursements/{id}', [DisbursementController::class, 'show']);
Router::get('/budgets/disbursements/{id}/edit', [DisbursementController::class, 'edit']);
Router::post('/budgets/disbursements/{id}', [DisbursementController::class, 'update']);
Router::post('/budgets/disbursements/{id}/delete', [DisbursementController::class, 'destroy']);

// Disbursement Items Routes
Router::get('/budgets/disbursements/{id}/items/create', [DisbursementController::class, 'createItem']);
Router::post('/budgets/disbursements/{id}/items', [DisbursementController::class, 'storeItem']);
Router::get('/budgets/disbursements/{id}/items/{itemId}/edit', [DisbursementController::class, 'editItem']);
Router::post('/budgets/disbursements/{id}/items/{itemId}', [DisbursementController::class, 'updateItem']);
Router::post('/budgets/disbursements/{id}/items/{itemId}/delete', [DisbursementController::class, 'destroyItem']);

// API for Hierarchical Dropdown
Router::get('/api/budget-plans/outputs', [DisbursementController::class, 'getOutputs']);
Router::get('/api/budget-plans/activities', [DisbursementController::class, 'getActivities']);
```

---

## 🎨 UI Features (จาก Wireframe)

### Part 1: Dashboard เลือกกิจกรรม
- แสดง Cards ของ Activities ทั้งหมด
- กดเลือก Activity → ไปหน้าฟอร์มบันทึก

### Part 2: ฟอร์มบันทึก (Tab Navigation)
- **5 Tabs**: งบบุคลากร | งบดำเนินงาน | งบลงทุน | งบเงินอุดหนุน | งบรายจ่ายอื่น
- **Hierarchical Selects**: แผนงาน → ผลผลิต/โครงการ → กิจกรรม
- **6 Input Fields**: รายการ 0-5 (ตามhierarchy ที่เพิ่งทำเสร็จ)
- **Color-coded Tabs**: แยกสีตามประเภทงบ

---

## 📊 Data Flow

### 1. สร้าง Header (Step 1)
```
User เลือก: ปีงบ + เดือน + หน่วยงาน + วันที่
  ↓
บันทึก → disbursement_headers
  ↓
ได้ header_id
```

### 2. เพิ่ม Details (Step 2)
```
User เข้าหน้า show.php (header_id)
  ↓
กดปุ่ม "เพิ่มรายละเอียด"
  ↓
เข้า items/create.php (Tab Form)
  ↓
User เลือก Tab (ประเภทรายจ่าย)
  ↓
กรอก: แผนงาน → ผลผลิต → กิจกรรม + รายการ 0-5
  ↓
บันทึก → disbursement_details
```

---

## 🔗 Related Tables (ที่มีอยู่แล้ว)

| Table | Status | Usage |
|-------|--------|-------|
| `organizations` | ✅ พร้อม | Select หน่วยงาน |
| `budget_plans` | ✅ พร้อม | Hierarchical: แผนงาน → ผลผลิต → กิจกรรม |
| `budget_category_items` | ✅ พร้อม | ใช้สำหรับ รายการ 0-5 (ถ้าต้องการ) |

---

## 🚀 ขั้นตอนการ Execute (แนะนำ)

### Phase 1: Database (30 นาที)
1. สร้าง migration files (3 ตาราง)
2. รัน migrations
3. Seed expense_types

### Phase 2: Models (30 นาที)
1. สร้าง 3 Models พร้อม CRUD methods

### Phase 3: Controller (1.5 ชม.)
1. สร้าง DisbursementController
2. Implement Header CRUD
3. Implement Detail CRUD
4. Implement API endpoints

### Phase 4: Views - Header (1 ชม.)
1. index.php (ตารางรายการ)
2. create.php (ฟอร์ม Header)
3. show.php (รายละเอียด + ตาราง Details)

### Phase 5: Views - Detail (2 ชม.)
1. items/create.php (Tab Form - ตาม wireframe)
2. items/edit.php
3. JavaScript สำหรับ Tab switching
4. JavaScript สำหรับ Hierarchical dropdown

### Phase 6: Routes & Testing (30 นาที)
1. เพิ่ม routes
2. ทดสอบ workflow ทั้งหมด

**รวมประมาณ: 5.5-6 ชั่วโมง**

---

## ⚠️ สิ่งที่ต้องระวัง

1. **Hierarchical Dropdown**: ต้องมี AJAX loading ผลผลิต/กิจกรรม
2. **Tab Navigation**: ต้อง save state (ถ้า user เปลี่ยน tab)
3. **Validation**: รายการ 0-5 ต้อง validate ว่ากรอกอย่างน้อย 1 field
4. **UI Consistency**: ใช้ Dark theme + Glassmorphism ให้สอดคล้องกับ wireframe

---

## 🎯 คำสั่งเริ่มต้นสำหรับ Chat ใหม่

```
สร้าง Disbursement Form (ฟอร์มบันทึกเบิกจ่าย) ตาม:
- Design: research/disbursement_form_workflow.md
- Wireframe: research/wireframe_disbursement_form.html
- เริ่มจาก Phase 1: Database (3 tables)
- ใช้ Tab Navigation สำหรับ 5 ประเภทงบ
- Hierarchical dropdown: แผนงาน → ผลผลิต → กิจกรรม
```

---

**End of Handoff**
