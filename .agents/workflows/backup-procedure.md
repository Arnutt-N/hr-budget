---
description: ขั้นตอนการสำรองข้อมูลโปรเจค (Project Backup Standard)
---

# 📦 มาตรฐานการสำรองข้อมูล (Backup Standard)

เพื่อให้การพัฒนาระบบมีความปลอดภัยและสามารถย้อนกลับได้เมื่อเกิดข้อผิดพลาด จึงกำหนดมาตรฐานการสำรองข้อมูลดังนี้:

## 🕒 เมื่อไหร่ต้อง Backup?
1. **จบ Phase**: เมื่อพัฒนาเสร็จสิ้นในแต่ละ Phase (ตาม Roadmap)
2. **Major Change**: ก่อนเริ่มทำการเปลี่ยนแปลงโครงสร้างใหญ่ หรือ Refactor โค้ด
3. **Weekly**: อย่างน้อยสัปดาห์ละ 1 ครั้ง (ถ้ามีการแก้ไข)

## 📂 สถานที่เก็บ (Location)
เก็บไว้ในโฟลเดอร์: `archives/backup/`
รูปแบบชื่อโฟลเดอร์: `hr_budget_[phase/event]_[YYYYMMDD]`

## 📝 คำสั่ง Backup
1. **สร้างโฟลเดอร์ปลายทาง** (ถ้ายังไม่มี)
2. **รันคำสั่ง Robocopy**

```cmd
mkdir "c:\laragon\www\hr_budget\archives\backup\hr_budget_[ชื่อเหตุการณ์]_[YYYYMMDD]"
robocopy "c:\laragon\www\hr_budget" "c:\laragon\www\hr_budget\archives\backup\hr_budget_[ชื่อเหตุการณ์]_[YYYYMMDD]" /E /XD node_modules vendor .git archives .gemini .agent /XF *.log
```

> [!IMPORTANT]
> **หากรันผ่าน Agent แล้วไม่ผ่าน** (เช่น Permission Error) ให้ **Request ให้ User รันด้วยตัวเอง** ผ่าน Terminal

## 🕵️‍♂️ การตรวจสอบ (Verification) **(สำคัญมาก)**
ต้องทำการตรวจสอบทุกครั้งหลังรันคำสั่ง ห้ามข้ามขั้นตอนนี้

1. **Check Directory Existence**: โฟลเดอร์ปลายทางต้องถูกสร้างขึ้นจริง
2. **Check File Count**: ต้องมีไฟล์ข้างใน (ไม่ใช่โฟลเดอร์เปล่า)
   - ใช้คำสั่ง: `dir "c:\laragon\www\hr_budget\archives\backup\...\src" /s`
   - ต้องเห็นรายการไฟล์จำนวนมาก
3. **Check Size**: ขนาดรวมต้องใกล้เคียงกับต้นฉบับ

## ✅ Checklist การ Backup
- [ ] 1. รันคำสั่ง Backup (Robocopy)
- [ ] 2. **Verification**: รันคำสั่ง `list_dir` หรือ `dir` ดูในโฟลเดอร์ปลายทาง
    - [ ] เห็นโฟลเดอร์ย่อย (เช่น `src`, `public`, `resources`) ครบถ้วน
    - [ ] ❌ **ถ้าว่างเปล่า = FAILED** ต้องหาสาเหตุหรือขอให้ User รันเอง
- [ ] 3. บันทึก Log ลงใน `project-log-md/backup-log.md` เมื่อตรวจสอบผ่านแล้วเท่านั้น
- [ ] 4. แจ้ง User พร้อม Location path ที่ตรวจสอบแล้ว